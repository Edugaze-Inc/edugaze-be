version: "3.8"
services:
  auth:
    image: edugaze.development:0.0.1
    environment:
      NODE_ENV: development
      PORT: 5000
    build:
      context: auth/
      target: dev
    ports:
      - 5000:3000
    volumes:
      - ./auth:/usr/src/app/
    depends_on:
      - auth-mongo
  meetings:
    image: edugaze.development:0.0.1
    environment:
      NODE_ENV: development
      PORT: 4000
    build:
      context: meetings/
      target: dev
    ports:
      - 4000:4000
    volumes:
      - ./meetings:/usr/src/app/
    depends_on:
      - auth-mongo

  auth-mongo:
    image: mongo
    ports:
      - 27017:27017
